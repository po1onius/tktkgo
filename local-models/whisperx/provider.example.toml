[server]
host = "127.0.0.1"
port = 8102
log_level = "INFO"

[model]
# WhisperX 官方中文默认对齐模型；模型卡许可证为 Apache-2.0。
name = "jonatasgrosman/wav2vec2-large-xlsr-53-chinese-zh-cn"
# 权重在启动时自动下载到本服务目录；也可以填写已经下载的本地绝对路径。
download_root = "./models"
device = "cpu"
language = "zh"
cpu_threads = 0
num_workers = 1
